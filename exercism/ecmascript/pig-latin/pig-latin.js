const vowels = new Set([ 'a', 'e', 'i', 'o', 'u'])
const consonants = new Set([
  'b', 'c', 'd', 'f', 'g', 'h', 'j', 'k', 'l', 'm', 'n',
  'p', 'q', 'r', 's', 't', 'v', 'w', 'x', 'y', 'z'
])

const isVowel = (letter) => vowels.has(letter)
const isConsonant = (letter) => consonants.has(letter)

const translateVowel

const translate = (word) => {
  let 
  return
}

export default {
  translate
}
